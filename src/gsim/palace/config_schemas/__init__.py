"""Vendored Palace configuration schemas used for local validation.

The JSON files in this package are snapshots of the official Palace
configuration schemas for the versions gsim explicitly supports. They are read
by the config validator after gsim has assembled a full ``config.json``; they do
not define ownership of mesh-derived sections such as ``Domains`` or
``Boundaries``.
"""
